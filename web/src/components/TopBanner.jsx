// src/components/TopBanner.jsx
export default function TopBanner() {
  return (
    <div
      className="top-banner"
      style={{
        height: "60px",
        margin: "8px 0",
        borderRadius: "10px",
        background: "#ffffff50",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontWeight: "600",
        color: "#555",
        boxShadow: "0 2px 6px rgba(0,0,0,0.1)"
      }}
    >
      AdMob Banner — banner-top
    </div>
  );
}
